package main

import "testing"

// We typically avoid spinning up the whole server in unit tests.
// This placeholder keeps the package covered and compile-checked.
func TestMainPackageCompiles(t *testing.T) {}
